import { Component } from '../EntityComponentSystem';
/**
 * Shim to draw to the canvas when in LegacyDrawing mode
 * @deprecated
 */
export declare class CanvasDrawComponent extends Component<'ex.canvas'> {
    draw?: (ctx: CanvasRenderingContext2D, delta: number) => void;
    readonly type = "ex.canvas";
    constructor(draw?: (ctx: CanvasRenderingContext2D, delta: number) => void);
}
